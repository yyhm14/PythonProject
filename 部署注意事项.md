# 部署注意事项

---

## 问题：系统定时任务启动脚本，日志/文件写入不执行

### 根本原因

系统定时任务（cron / systemd）启动脚本时，**工作目录不是脚本所在目录**，
导致使用相对路径写文件时，路径指向错误位置，文件写入失败或无权限。

```
手动运行：
  cd /home/user/project && python alarm_service.py
  工作目录 = /home/user/project/
  写 "alarm.log" → /home/user/project/alarm.log  ✅

系统定时任务自动运行：
  工作目录 = /  或  /root  等其他目录
  写 "alarm.log" → /alarm.log  ❌ 没有权限，写入失败
```

---

### 解决方案

在脚本顶部加入以下代码，后续所有文件路径统一使用绝对路径：

```python
import os

# 获取脚本自身所在目录的绝对路径
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# 使用示例
LOG_FILE   = os.path.join(BASE_DIR, "alarm.log")
STATS_FILE = os.path.join(BASE_DIR, "statistics.json")
```

---

### 原来写法 vs 修正写法

```python
# ❌ 原来（相对路径，系统定时任务下会出问题）
with open("alarm.log", "a", encoding="utf-8") as f:
    f.write("...")

with open("statistics.json", "w", encoding="utf-8") as f:
    json.dump(data, f)

# ✅ 修正（绝对路径，任何方式启动都正确）
with open(LOG_FILE, "a", encoding="utf-8") as f:
    f.write("...")

with open(STATS_FILE, "w", encoding="utf-8") as f:
    json.dump(data, f)
```

---

### 影响范围

凡是脚本中涉及以下操作，都需要改成绝对路径：

| 操作 | 涉及文件 | 是否受影响 |
|------|---------|----------|
| 写日志 | alarm.log 等 | ✅ 受影响 |
| 读写统计文件 | statistics.json | ✅ 受影响 |
| 读写配置文件 | config.json 等 | ✅ 受影响 |
| 飞书 API 调用 | 无本地文件 | ❌ 不受影响 |
| 内存变量操作 | 无本地文件 | ❌ 不受影响 |

---

## 服务重启后数据恢复

加入 `BASE_DIR` 后，`statistics.json` 始终写入脚本同目录。

服务重启流程：

```
系统定时任务重启脚本
        ↓
main() 启动
        ↓
DailyStatistics.__init__() 执行
        ↓
load_from_file() 读取 statistics.json
        ↓
内存数据恢复 ✅ 统计数据不丢失
```

只要 `statistics.json` 文件存在，重启后统计数据完全恢复，当天的推送数量和填写数量不会归零。
