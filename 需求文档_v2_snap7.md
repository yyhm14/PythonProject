# 下一阶段需求文档 — 报警管理平台 v2

---

## 一、背景

当前系统（v1）通过 SCADA VBS 脚本触发 HTTP 请求向 Python 服务推送报警，存在以下局限：

| 问题 | 影响 |
|------|------|
| VBS 依赖 SCADA 监控进程运行 | 监控未启动时漏报 |
| 网络抖动导致偶发漏推 | 无本地队列，失败即丢失 |
| 报警字段配置在 VBS 代码里 | 每次新增/修改报警需改代码 |
| 数据存储在 JSON 文件 | 无法多人查询、统计分析困难 |

v2 目标：**Python 直连 PLC，配置可视化，数据入库，彻底脱离 VBS 依赖。**

---

## 二、整体架构

```
Siemens PLC（生产网）
    ↓  snap7 轮询（1-3秒）
Python 采集服务
    ↓  边沿检测（0→1 触发）
报警队列（本地，持久化）
    ↓
飞书推送模块（复用 v1 逻辑）
    ↓
MySQL 数据库（统一存储）
    ↑
Web 配置界面（浏览器访问）
```

---

## 三、功能需求

### 3.1 PLC 直连采集

- 使用 `python-snap7` 库通过 S7 协议读取 PLC 数据
- 单独建一个采集 DB 块（如 DB100），由 PLC 程序将需要上报的信号镜像写入，不侵入原有控制逻辑
- 采集 DB 结构（每个报警点）：

| 偏移 | 类型 | 说明 |
|------|------|------|
| +0.0 | Bool | 报警位（PLC 写入，0→1 触发） |
| +2 | Int | 设备编号（对应配置表） |
| +4 | Int | 故障编号（对应配置表） |

- 边沿检测：记录上次状态，仅在 0→1 跳变时触发推送
- 断线自动重连，重连期间检测到的状态变化在恢复后补发

### 3.2 本地报警队列

- 报警触发后先写入本地队列（SQLite 或 JSON），再异步推送飞书
- 推送成功后标记完成，失败自动重试（间隔 30 秒，最多 10 次）
- 服务重启后未完成的队列自动继续处理，彻底解决漏推问题

### 3.3 Web 配置界面

- 浏览器访问（如 `http://localhost:8080`），无需安装客户端
- **设备管理**：增删改设备信息（设备名、线体、DB地址、位偏移）
- **故障字典**：维护故障编号与故障描述的对应关系
- **屏蔽规则**：查看和手动管理屏蔽记录
- **实时状态**：显示各设备当前报警状态、采集服务运行状态
- 配置修改后无需重启服务，热加载生效

### 3.4 MySQL 数据库

替换现有 JSON 文件存储，统一管理所有数据：

| 表名 | 内容 |
|------|------|
| `alarm_config` | 设备与 PLC DB 地址的映射配置 |
| `fault_dict` | 故障编号与故障描述字典 |
| `alarm_records` | 每条报警的完整记录（时间/设备/故障/处理人/状态）|
| `suppression_rules` | 屏蔽规则及到期时间 |
| `alarm_queue` | 待推送队列（用于断网重试）|

### 3.5 飞书推送（复用 v1）

- 保留 v1 的所有飞书交互逻辑（防重复、卡片交互、闭环填写、日报）
- 数据写入目标从 JSON 文件改为 MySQL
- 接口签名不变（`/receive_data`），兼容仍在运行的 VBS 脚本过渡期使用

---

## 四、技术选型

| 模块 | 技术 | 说明 |
|------|------|------|
| PLC 通信 | `python-snap7` | 支持 S7-200/300/400/1200/1500 |
| Web 框架 | Flask（已有）+ Jinja2 | 扩展现有服务，新增 Web 路由 |
| 前端 | Bootstrap 5 + 原生 JS | 轻量，无需前端构建工具 |
| 数据库 | MySQL 8.0 | 或云数据库（阿里云RDS等） |
| ORM | `pymysql` + 手写 SQL | 简单直接，避免引入重框架 |
| 任务队列 | Python threading（已有）| 复用现有线程模型 |

---

## 五、实施计划

| 阶段 | 内容 | 预计周期 |
|------|------|---------|
| Phase 1 | snap7 采集 + 本地队列，替换 VBS 触发 | 1-2 周 |
| Phase 2 | MySQL 接入，替换 JSON 文件存储 | 1 周 |
| Phase 3 | Web 配置界面（设备管理 + 故障字典）| 1-2 周 |
| Phase 4 | 统计分析页面、历史查询 | 按需 |

---

## 六、前置条件

- [ ] 确认 PLC 型号（S7-1200 / S7-1500 / 其他）
- [ ] 获取 PLC IP 地址和机架/槽号
- [ ] 与电气工程师确认可新建采集 DB 块
- [ ] 确认 MySQL 部署位置（本地 / 云端）及连接信息
- [ ] 确认 Web 界面访问范围（仅本机 / 局域网内）
